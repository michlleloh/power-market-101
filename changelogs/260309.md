# Change Log — Week of Mon 9 Mar – Sun 15 Mar 2026

---

## 2026-03-14

### `CLAUDE.md` — Documentation overhaul

- Added required Claude Code guidance prefix to top of file
- Added `## Preview / Testing` section explaining how to open and validate the file
- Added `const TOTAL` and `const COUNTRY_ORDER` to the File Architecture table — both exist in the script but were undocumented
- Replaced the single Functions row in File Architecture with a dedicated `### Key Functions` table covering all 13 functions (`buildSb`, `buildCur`, `openPanel`, `closePanel`, `ckClick`, `markBtn`, `upd`, `openSb`, `closeSb`, `openGm`, `closeGm`, `buildFbar`, `renderGm`, `setF`, `mkTag`)
- Added a note in `## Comparison Tables` clarifying that the `compare` key in `const FILTERS` is a pseudo-filter and must not be added to `const C`
- Added `## Change Log Files` section establishing the weekly log file convention (`changelogs/YYMMDD.md`, Monday-dated, always update after changes)
- Added v5 entry to the in-file `## Change Log` section
- Updated file naming convention from `YYYY-MM-DD` to `YYMMDD`; renamed this file from `2026-03-09.md` to `260309.md`
- Renamed main HTML file from `energy-markets-training.html` to `power-market-101.html`; updated all three references in `CLAUDE.md`
