# Change Log — Week of Mon 9 Mar – Sun 15 Mar 2026

---

## 2026-03-14 (continued 3)

### `power-market-101.html` — Visual polish pass (annotations batch 2)

- Sidebar glossary button: equal spacing above and below — `.sb-sep` margin increased to `1rem` top/bottom; `.sb-head` padding-bottom increased to `1rem`
- Sidebar session items: horizontal padding increased `1.1rem → 1.4rem` for more space from sidebar edges
- Glossary filter tabs: added `padding-top:.6rem` to `.fbar` so spacing is equal above and below the tab row; `#gm-srch` margin changed to `.6rem 0` to match; `#gm-body` padding-top reduced to `.6rem` — all three gaps now equal
- Glossary filter tabs: horizontal padding reduced `0.38rem → 0.22rem` so chips fit tighter to their text
- Compare table country tags: added `.cmp-tbl .tag{padding:.18rem}` override — equal padding on all four sides, width now follows text content
- Close buttons: `× Close` in `#gm-x` simplified to `×` only — matches `#panel-x` and `.sb-close`
- Hero bottom padding reduced `1.5rem → 0.5rem` and description paragraph `margin-bottom:0` added — brings hero border-bottom closer to sidebar head border-bottom (cyan alignment)
- Hero description: `max-width:500px` removed — text spreads to full column width, contributing to hero height reduction
- Main content bottom padding reduced `3rem → 2rem` to reduce excessive whitespace below last module
- `openPanel`: added `setTimeout(300)` to measure hero bottom Y after panel slides in, then sets `padding-top` on the KEY ABBREVIATIONS `.blk` to align it with the hero border-bottom — applies to all sessions dynamically
- Mobile topbar title text changed from "Energy Markets" to "View all modules"; made tappable (`onclick="openSb()"`)
- Mobile topbar glossary button text changed from "[Glossary] Glossary" to "Glossary"
- Mobile `#main` horizontal padding increased `.85rem → 1.1rem` at `max-width:479px`

---

## 2026-03-14 (continued 2)

### `power-market-101.html` — Notion-style dynamic layout and panel alignment

- Removed `max-width` constraint from `#main`; `.main-inner` already bounds text width at `var(--main-max)`
- Added `transition:margin-right .25s` to `#main` and `#main.panel-open{margin-right:var(--panel)}` — when a session panel opens, main content shifts left to sit flush alongside the panel with no overlay
- Added `#main.panel-open{margin-right:0!important}` override for mobile breakpoints where the panel is full-screen
- `openPanel`: adds `panel-open` class to `#main`; overlay only shown on `window.innerWidth < 768`
- `closePanel`: removes `panel-open` class from `#main`
- `#panel` top padding changed from `clamp(1.5rem,4vw,2rem)` → `1.1rem` — matches `#main` top padding so content in both columns starts at the same height

---

## 2026-03-14 (continued)

### `power-market-101.html` — Layout, spacing, and glossary polish

- Removed `margin-top:.75rem` from `.sb-gloss` — separator's own `.75rem` bottom margin now provides equal spacing above and below the glossary button
- Widened sidebar: base `--sb` from `220px` → `260px`; 1024–1439px breakpoint `220px` → `240px`; 1440px breakpoint `240px` → `260px`; 1920px breakpoint `260px` → `280px`
- Increased `.sb-item` horizontal padding from `.9rem` → `1.1rem` for more breathing room between session text and sidebar edges
- Reduced `.sb-nav` bottom padding from `2rem` → `1.1rem` to match `.sb-head` top padding
- Reduced `#main` bottom padding from `6rem` → `3rem` to remove excessive white space below curriculum
- `#gm-srch` top margin added (`.5rem`) — now `margin:.5rem 0` so spacing is equal above and below the search input
- Added `#gm-srch::placeholder` rule: `font-style:italic; opacity:.5` for visual distinction
- Reduced `.fb` (glossary filter tab) horizontal padding from `.45rem` → `.38rem` so chips sit closer to their text label

---

## 2026-03-14

### `CLAUDE.md` — Documentation overhaul

- Added required Claude Code guidance prefix to top of file
- Added `## Preview / Testing` section explaining how to open and validate the file
- Added `const TOTAL` and `const COUNTRY_ORDER` to the File Architecture table — both exist in the script but were undocumented
- Replaced the single Functions row in File Architecture with a dedicated `### Key Functions` table covering all 13 functions (`buildSb`, `buildCur`, `openPanel`, `closePanel`, `ckClick`, `markBtn`, `upd`, `openSb`, `closeSb`, `openGm`, `closeGm`, `buildFbar`, `renderGm`, `setF`, `mkTag`)
- Added a note in `## Comparison Tables` clarifying that the `compare` key in `const FILTERS` is a pseudo-filter and must not be added to `const C`
- Added `## Change Log Files` section establishing the weekly log file convention (`changelogs/YYMMDD.md`, Monday-dated, always update after changes)
- Added v5 entry to the in-file `## Change Log` section
- Updated file naming convention from `YYYY-MM-DD` to `YYMMDD`; renamed this file from `2026-03-09.md` to `260309.md`
- Renamed main HTML file from `energy-markets-training.html` to `power-market-101.html`; updated all three references in `CLAUDE.md`

### `power-market-101.html` — UI copy and layout tweaks

- Hero h1 changed to "Energy & Power Market 101"
- Hero subtitle changed to "32 sessions · 11 modules"; long market list removed
- Added a small how-to description paragraph inside the hero section
- Sidebar logo changed to "Energy & Power Market 101"; "Aurora-level" removed from sub-label
- Removed the fixed `#header` bar entirely (CSS block, HTML element, and all mobile overrides)
- Reverted `#sb` to `top:0; height:100vh` and `#main` padding to original values
- Removed `[i]` icon span from the tip box
- Removed `[Glossary]` prefix from the glossary open button
- Removed `[Glossary]` prefix from the glossary modal title ("Full Glossary")
- Increased `.sb-mod` top padding from `0.55rem` to `1.1rem` for more breathing room between modules

### `power-market-101.html` — Description alignment and glossary button relocation

- Moved hero description into the `.tip` box (replacing old tip text) so both align to the same block edge
- Removed standalone description paragraph from inside `.hero`; hero now shows h1 + stats only
- Removed `#btn-gloss` from main content area
- Added `.sb-gloss` glossary button inside `.sb-head` (below progress line) so it stays locked in view when scrolling the sidebar
- Added `.sb-gloss` and `.sb-gloss:hover` CSS rules

### `power-market-101.html` — Copy, styling and glossary refinements

- Added `<hr class="sb-sep">` between progress line and glossary button; equal `.65rem` gap above and below the separator
- `.sb-gloss` restyled as plain text link (no box border); hover darkens text instead of changing background
- Moved description text back into `.hero` as a smaller plain `<p>`; removed `.tip` styled box entirely
- Changed "32 sessions · 11 modules" to "11 modules · 32 sessions" in sidebar sub-label and hero stats (all instances)
- Reordered glossary filter tabs: All → Global → SGP → PHL → TWN → MYS → KOR → CHN → Compare
- Widened country tag colour gradient for more visual distinction between each step (CHN `#d49500` → SGP `#fde87a`); all values verified WCAG AA compliant with `#4a2d00` text
- Removed "core Aurora analyst knowledge" from compare view intro text
- Increased `#gm-body` bottom padding from `1.3rem` to `2rem`; added `.75rem` top padding for even spacing

### `power-market-101.html` — Alignment, tags, and spacing polish

- `.sb-sep` now spans full sidebar width via `margin:.75rem -.9rem` (breaks out of `.sb-head` padding); equal `.75rem` gap above and below
- `.sb-gloss` restored as a box button with `border:1px solid var(--border)` and `border-radius:4px`
- `#main` padding changed to `1.1rem .9rem 6rem` — matches `.sb-head` top (`1.1rem`) and left (`.9rem`) so hero aligns with sidebar logo
- All `.tag` chips: switched to `font-family:monospace`, removed `letter-spacing`, added `min-width:2.6rem` and `display:inline-flex` for consistent sizing
- Compare market squares: `font-family:monospace`, `min-width:3.4rem`, `display:inline-flex` with centered text — all country chips now same size
- `.cmp-tbl th` and `.cmp-tbl td` both set to `vertical-align:middle`
- `.cmp-intro` margin changed from `.85rem 0 1rem` to `1rem 0` for equal spacing above and below
- `.gm-head` top padding increased from `1.1rem` to `1.3rem` to match side padding
- `#gm-body` bottom padding removed from container; replaced with `.ge:last-child{margin-bottom:1.3rem}` and `.cmp-c:last-child{margin-bottom:1.3rem}` to fix the flex scroll clipping bug that was preventing bottom spacing from rendering

### `power-market-101.html` — Critical bug fix: panel blocking session clicks

- `#panel` was still referencing `var(--header)` after `--header` was removed from `:root`; undefined CSS custom property made `top` and `height` invalid, causing the panel to render at an unpredictable position and intercept clicks on session rows
- Fixed: `#panel` now uses `top:0; height:100vh` explicitly
- Confirmed `var(--header)` is completely absent from all CSS rules
- Reverted `.tag` from `display:inline-flex` (which also caused click interference) to `display:inline-block`; monospace font retained

### `power-market-101.html` — UI tweaks

- Removed lightning emoji (`&#9889;`) from all instances: browser tab title, sidebar logo, mobile topbar, hero section
- Added fixed full-width `#header` bar (44px, `--header` CSS variable) spanning the full viewport edge-to-edge
- Header uses `background:var(--bg2)` with `border-bottom` and subtle `box-shadow` for a framed, grounded appearance
- Adjusted `#sb`, `#panel` to start at `top:var(--header)` and use `height:calc(100vh - var(--header))`
- Adjusted `#main` top padding to account for the header height
- Increased `#panel` bottom padding from `3rem` to `4rem` to fix text cutoff at scroll bottom
- Mobile breakpoints (`max-width:767px`): header hidden, sidebar and panel reset to full viewport height
